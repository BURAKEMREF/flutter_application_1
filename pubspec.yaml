name: flutter_application_1
description: A new Flutter project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase paketleri (uyumlu versiyonlarla)
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.0
  firebase_storage: ^12.3.7

  # Dosya seçimi için
  image_picker: ^1.1.0

  # Swipe kartlar için
  card_swiper: ^2.0.4

  # Benzersiz ID üretimi için
  uuid: ^3.0.6
  video_player: ^2.7.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    -  assets/default_avatar.png  # Varsayılan profil resmi